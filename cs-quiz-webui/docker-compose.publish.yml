# Optional overlay: host port publish (local / direct bind).
# Scripts add -f docker-compose.publish.yml when publish_port is set.

services:
  cs-quiz-webui:
    ports:
      - target: ${INTERNAL_PORT:-80}
        published: ${PUBLISH_PORT:-5173}
        protocol: tcp
