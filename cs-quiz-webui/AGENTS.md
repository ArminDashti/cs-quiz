# AGENTS.md

## Auth

- Default local login (seeded by `cs-quiz-api` on startup):
  - username `armin` / password `dopadopa123`
  - username `admin` / password `dopadopa123`
- Login UI uses username + password (not email).

## Learned User Preferences

- Always use default login username `armin` and password `dopadopa123` for local/demo api-webui auth seeding and sign-in (also seed `admin` / `dopadopa123`).
